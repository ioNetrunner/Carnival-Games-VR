using UnityEngine;
using System.Collections;

public class BallTossRoomController : MonoBehaviour {

	/*


	// Use this for initialization
	void Start () {
		
		numBalls = 3;

	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
		
	public int getNumBalls(){
		return numBalls;
	}


	public void decrementNumBalls()
	{
		numBalls = (numBalls - 1);

		if (numBalls < 0) numBalls = 0;
	}

*/
}

